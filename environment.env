# Backend API Configuration
# Set this to your Node.js backend URLt.ai

NODE_BACKEND_URL=http://10.240.53.65:3002

# API Configuration
NODE_API_URL=http://10.240.53.65:3002
PYTHON_API_URL=http://10.240.53.65:8000
API_TIMEOUT=60

# Database Configuration
# Windows Authentication (NTLM) - similar to NestJS app.module.ts
DB_SERVER=10.240.10.202
DB_PORT=5555
DB_NAME=NEWDCC-V4-UAT
DB_DRIVER=ODBC Driver 18 for SQL Server

# Windows Authentication Settings (matching NestJS app.module.ts)
# Bank provided: ADIBEG\GRCSVC account with Windows Authentication enabled
# 
# ✅ CONFIGURED FOR WINDOWS HOST (Option 2)
# 
# IMPORTANT: You MUST run Python as ADIBEG\GRCSVC user for this to work!
# 
# How to run:
#   1. Open Command Prompt or PowerShell
#   2. Run: runas /user:ADIBEG\GRCSVC python main.py
#   3. Enter password when prompted: P@ssw0rd
#   OR
#   - Right-click on Python script → "Run as different user"
#   - Enter: ADIBEG\GRCSVC and password
#
# This will use Windows Authentication (Trusted_Connection=yes)
# Works on Windows host only, NOT in Docker containers
DB_USE_WINDOWS_AUTH=yes

# For Windows Authentication with domain credentials (if needed)
# Leave empty to use current Windows user, or specify domain\username
DB_DOMAIN=ADIBEG
DB_USERNAME=GRCSVC
DB_PASSWORD=P@ssw0rd

# Connection Settings
DB_ENCRYPT=yes
DB_TRUST_SERVER_CERTIFICATE=yes
DB_REQUEST_TIMEOUT=60000
DB_CONNECT_TIMEOUT=60000

# Connection Pool Settings (similar to NestJS)
DB_POOL_MAX=20
DB_POOL_MIN=5
DB_POOL_ACQUIRE=60000
DB_POOL_IDLE=30000